# NOPaxos Sequencer Configuration File
# This file configures the endhost sequencer
# Updated for Docker host networking mode

# Network interface name (eth0 on the host)
interface eth0

# Multicast group address (must match the multicast address in replica config)
groupaddr 224.0.0.1

